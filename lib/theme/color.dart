
import 'package:flutter/material.dart';
class ThemeColor{
    ///任务未完成的颜色
    static Color taskNotStart = Color(0xFF50DBAE);

    ///任务正在进行的颜色
    static Color taskRunning = Color(0xFF32BFD0);

    ///任务结束的颜色
    static Color taskFinished = Color(0xFF53B53F);

    ///底部导航栏文字颜色
    static Color navTextColor = Color(0xFF949292);

    ///底部导航栏图标颜色
    static Color navIconColor = Color(0xFF929292);

    //底部导航栏选中颜色
    static Color navSelectColor = Color(0xFF40996D);




}

class BaseColor{
    static Color white = Color(0xFFFFFFFF);

    static Color redD98080 = Color(0xFFD98080);

    static Color green65C6A6 = Color(0xFF65C6A6);

    static Color greyCCCCCC = Color(0xFFCCCCCC);
}
